server {
    server_name {{hostname}};

	location / {
        proxy_pass         http://localhost:{{sites[0].actual_port}};
        proxy_redirect     off;
        proxy_set_header   Host $host;
	}
}
