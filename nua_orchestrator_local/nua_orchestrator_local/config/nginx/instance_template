server {
	location /{{prefix}} {
		rewrite /{{prefix}}(/.*|$) /$1  break;
        proxy_pass         http://localhost:{{port}};
        proxy_redirect     off;
        proxy_set_header   Host $host;
	}
}
